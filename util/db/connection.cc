#include "connection.h"
#include "psql_connection.h"

namespace DBase {

//Connection::Connection(std::string _conn_info) throw (ConnectionFailed) : conn_info(_conn_info), transaction_active(false) {
//}

Connection::~Connection() {
}

}
